Layout:

- scrolling inside Layoutcontainer

- improving layoutSolver for better adding/removing
- add/remove layout elements dynamically

Widgets:

- all UIElements should have optional parent and childs
- simple scrollbar

Skin:

- ui-elements inside a container needs to be masked



Mouse- and Multitouch:

- handle touchevents and -points
- let emulate touch-events for mouse-only-targets


Bubbling:

- if bubbling are enabled for picked ui-element then pass it throught to the parent-uiElement

